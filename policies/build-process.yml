_schema: https://kosli.com/schemas/policy/environment/v1

artifacts:
  provenance:
    required: true
      
  trail-compliance:
    required: true

  attestations:
    - if: ${{ flow.tags.kind == "build" }}
      name: '*'
      type: pull_request
