_schema: https://kosli.com/schemas/policy/environment/v1

artifacts:
  trail-compliance:
    required: true
    exceptions:
      # Waive trails whose flow targets a different environment.
      # The exists() guard means a flow with no env tag is NOT waived.
      - if: ${{ exists(flow.tags.env) and flow.tags.env != "aws-prod" }}
